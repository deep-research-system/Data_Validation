logic_prompt = """
너는 한국어 설문지(md/txt)에서 '이동 로직(☞, ⇒, →, ~로 이동, 설문 종료 등)'만 추출한다.
절대 정규화/가공/표준화하지 마라. 단, [범위 절단 규칙]은 예외로 반드시 적용한다.

[출력 규칙]
- 출력은 반드시 JSON 배열만.
- 각 원소는 {"start": str, "value": [int,...], "end": str} 형식.
- value는 항상 정수 리스트. 단일도 [1].

[가장 중요한 5가지 — 이것만 수행]
1) 이동 로직(문항 이동/건너뛰기/종료) 문장만 식별한다.
2) start는 "이동 로직이 붙어있는 바로 그 질문의 문항ID 토큰"을 md 원문에서 그대로 가져온다.
3) end는 "도착 문항ID 토큰"을 md 원문에서 그대로 가져온다.
4) value는 보기 번호(①②③…)를 정수로 변환해 리스트로 넣는다.
5) start/end는 md 원문에 등장한 토큰 스타일을 그대로 유지한다. (문3/3-1 혼재 그대로)

[절대 금지]
- start/end에 질문 문장 전체를 넣지 마라.
- start/end를 네가 새로 만들지 마라.
- start를 큰 헤더(예: "문3")로 대표화하지 마라.
  (가지 문항이 있으면 무조건 가지 문항이 start다)

=========================================================
[start 결정 규칙 — 이것이 최우선]
이동 로직이 있는 줄(보기 줄 포함)을 기준으로 start를 결정한다.

A) 이동 로직이 "3-1.", "4-1.", "12." 처럼 가지 문항 라인(숫자/하이픈 형태) 아래의 보기(①②③...)에 달려있다면
   start는 그 가지 문항ID 토큰이다.
   - 예: "3-1. ... ① ... ☞ 3-1-1로 이동" => start="3-1" (절대 "문3" 아님)

B) 이동 로직이 큰 헤더(예: "### 문14.") 바로 아래 보기(①②③...)에 직접 달려 있고,
   그 사이에 "3-1." 같은 가지 문항 라인이 없다면
   start는 그 큰 헤더의 문항ID 토큰이다.
   - 예: "### 문14. ... ① 예 ☞ 문14-1..." => start="문14"

C) 위 A/B가 모두 애매하면,
   이동 로직이 있는 줄 바로 위쪽에서 가장 가까운 "문항ID 토큰" 하나를 start로 선택한다.
=========================================================

[end 결정 규칙]
- 이동 로직 문장 안에서 도착 "문항ID 토큰"을 찾아 end로 넣는다.
- end에는 반드시 '문항ID 토큰'만 들어가야 한다. ("~로 이동" 같은 문장 조각 포함 금지)

[범위 절단 규칙 — 반드시 적용(예외)]
- 원문이 "X ~ Y로 이동" 또는 "X~Y"처럼 범위 표기면 end는 무조건 X만 사용한다.
  - 예: "문14-1 ~ 문14-3로 이동" => end="문14-1"
  - 예: "문4-3~문4-6로 이동"     => end="문4-3"
  - 예: "4-3~4-5로 이동"         => end="4-3"
- 범위 표기의 공백 유무는 무시하되, end로 출력하는 X 토큰 자체는 원문에 나온 형태 그대로 출력한다.

[value 규칙]
- 보기 번호를 추출한다: ①=1, ②=2, ③=3, ④=4, ⑤=5 ...
- 해당 보기 줄에 이동 로직이 있으면 value=[해당 번호].
- 보기 번호가 없는 단독 이동 지시문이면 value=[].

[출력 예시]
[
  {"start":"3-1","value":[1],"end":"3-1-1"},
  {"start":"3-1","value":[2],"end":"3-2"},
  {"start":"문14","value":[1],"end":"문14-1"}
]
"""