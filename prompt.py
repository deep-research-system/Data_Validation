logic_prompt = """
너는 설문지 md 텍스트에서 "이동 로직(조건부 문항 이동/건너뛰기/종료)"만 추출한다.
출력은 JSON 배열만 허용한다. 설명/문장/추가 텍스트 금지.

[이동 로직으로 인정하는 패턴]
아래 표현이 포함된 줄(또는 바로 이어지는 줄)은 모두 "이동 로직 후보"다. 누락 없이 전부 찾아라.
- "로 이동", "이동)", "⇒", "→", "☞", "건너뛰", "다음 문항", "설문 종료", "종료"

[가장 중요한 5가지 — 이것만 수행]
1) 위 패턴이 있는 모든 이동 로직 문장을 빠짐없이 식별한다.
2) start: 이동 로직이 속한 질문의 "문항ID 토큰"을 md 원문에서 그대로 가져온다. (가공/정규화 금지)
3) end: 이동 대상의 "도착 토큰"을 md 원문에서 그대로 가져온다. (가공/정규화 금지)
4) value: 보기 번호를 정수로 추출하고 항상 정수 리스트로 출력한다. (단일도 [1])
5) start/end는 절대 문장 전체가 아니라 "토큰 그대로"여야 한다. (문장 넣으면 오답)

[토큰 추출 규칙: 절대 정규화/가공 금지]
- start/end는 md 원문에 실제로 등장한 형태 그대로의 토큰만 쓴다.
- 예: md에 "문14-1"로 쓰였으면 "문14-1" 그대로.
- 예: md에 "3-1-1"로 쓰였으면 "3-1-1" 그대로.
- 예: md에 "문4"로 쓰였으면 "문4" 그대로.

[start 결정 규칙 — 핵심]
이동 로직 후보 줄에서 start는 아래 규칙으로 결정한다.
A) 후보 줄 바로 위(또는 위쪽 근처)에서 가장 가까운 "문항ID 토큰"을 start로 잡는다.
B) 큰 헤더(예: "### 문14.") 아래의 보기 줄들(①②③...)에 이동이 달려 있으면 start는 그 헤더의 문항ID 토큰(예: "문14")이다.
C) 큰 헤더 아래에 "3-1. ..." 같은 가지 문항이 있고 그 줄/보기에서 이동이 달려 있으면 start는 가지 문항ID 토큰(예: "3-1")이다.
※ start는 절대 "문항 문장 전체"가 아니다. "문3", "3-1", "문14" 같은 ID 토큰만.

[value 규칙]
- 보기 번호(①=1, ②=2, ③=3, ④=4, ⑤=5 …)를 추출한다.
- 해당 이동 로직이 특정 보기(①/②/③...)에 붙어 있으면 그 번호가 value.
- 항상 리스트로 출력: [1], [2], [4] …
- value를 확정할 수 없는 이동(예: 보기 없이 단독 지시문)은 value를 []로 출력.

[end 결정 규칙]
- 후보 줄(또는 바로 이어지는 줄)에서 이동 대상 토큰을 찾아 end로 쓴다.
- "x ~ y" 범위면 end는 x 토큰만 쓴다. (예: "문14-1 ~ 문14-3" → end="문14-1")
- "문14-1 ~ 문14-3로 이동", "3-1-1로 이동", "(⇒ A38. 이동)" 등 표기 다양해도,
  md 원문에서 보이는 도착 토큰을 그대로 가져와라.
- 도착 토큰이 "다음 문항", "설문 종료"처럼 문항ID가 아닌 문구인 경우:
  그 문구 자체를 md 원문 그대로 end에 넣어라. (예: end="설문 종료")

[절대 금지]
- start/end에 질문 문장(텍스트 전체) 넣기
- start/end를 네가 새로 만들거나 정규화하기 (예: "문 14" → "문14" 같은 수정 금지)
- value를 정수 단일값으로 출력하기 (반드시 리스트)

[출력 형식]
아래 JSON 배열만 출력한다. (다른 텍스트 금지)

[
  {"start": "문2", "value": [1], "end": "문2-7-2"}
]
"""